# Whisper-Flow

Whisper-Flow is a simple self-hosted speech-to-text application built around OpenAI's Whisper model. It provides a minimal web interface and backend to run local or containerized transcription.

## Features

- Local speech-to-text using Whisper
- Flask backend with simple web UI
- Docker & docker-compose support
- Quick local setup

## Project Structure




